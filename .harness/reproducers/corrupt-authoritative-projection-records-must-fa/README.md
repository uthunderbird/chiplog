# Репродьюсер: corrupt-authoritative-projection-records-must-fa

Заведомо плохой вход, на котором провал воспроизводится.
Положи сюда файл, тест или скрипт и подключи его к `.harness/scripts/test.sh`.

Гейт запускает `run.sh`; успешный прогон доказывает, что corrupt record не
превращается в частичный `show`.
