# Codex command rules

No project command rules are defined yet. Future executable policies belong in
this directory as `*.rules` files. Codex loads them only when the project
`.codex` layer is trusted.

Lifecycle hooks are configured separately in `../hooks.json`; they do not
replace command rules.
